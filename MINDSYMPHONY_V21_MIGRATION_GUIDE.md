# MindSymphony v21.0 迁移指南

**版本**: 21.0.0-evolution
**更新**: 2025-01-11
**适用**: 从 v20.x 升级到 v21.0

---

## 📋 目录

1. [升级概览](#升级概览)
2. [变更对比](#变更对比)
3. [迁移步骤](#迁移步骤)
4. [功能详解](#功能详解)
5. [最佳实践](#最佳实践)
6. [常见问题](#常见问题)

---

## 升级概览

### 核心理念转变

| 维度 | v20.1 | v21.0 |
|------|-------|-------|
| **定位** | 配置驱动的技能库 | 进化的智能生态系统 |
| **触发方式** | skill_router + commands | 统一触发层 |
| **能力** | 静态技能集合 | 自我进化的智能体 |
| **协作** | 信息素协议 | Skills-n8n 智能协作 |

### 三大核心升级

```
┌─────────────────────────────────────────────────────────┐
│                  v21.0 核心架构                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  1. 统一触发层 (Unified Triggers)               │   │
│  │     - 合并 skill_router + commands              │   │
│  │     - 4种触发模式：命令/语义/模式/上下文          │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  2. 进化协议层 (Evolution Protocol)             │   │
│  │     - 自我学习：模式优化、用户适应               │   │
│  │     - 环境适应：项目检测、规范学习               │   │
│  │     - 协作进化：协同分析、集体智能               │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  3. Skills-n8n 协作层 (Collaboration Layer)     │   │
│  │     - 智能路由：自动选择 Skills 或 n8n          │   │
│  │     - MCP 桥接：双向通信机制                    │   │
│  │     - 协作模板：常见混合模式                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 变更对比

### 1. 触发系统变更

#### v20.1 配置

```yaml
# 分离的配置
skill_router:
  patterns:
    - pattern: "论文|学术|IMRaD"
      skill: "scientific-writing"
    - pattern: "引用|BibTeX"
      skill: "citation-management"

commands:
  - alias: "/paper"
    skill: "scientific-writing"
  - alias: "/cite"
    skill: "citation-management"
```

**问题**：
- ❌ 配置重复
- ❌ 维护成本高
- ❌ 缺少智能匹配

#### v21.0 配置

```yaml
# 统一的配置
unified_triggers:
  skill_triggers:
    - skill: "scientific-writing"
      triggers:
        - type: "command"
          patterns: ["/paper", "/论文"]
          weight: 10.0
        - type: "semantic"
          intent: "academic_writing"
          examples: ["帮我写论文", "生成研究草稿"]
          weight: 8.0
        - type: "pattern"
          regex: "论文|学术|IMRaD"
          weight: 5.0

      learning:
        track_usage: true
        optimize_patterns: true
```

**优势**：
- ✅ 单一配置源
- ✅ 多维度触发
- ✅ 自动学习优化

---

### 2. 进化能力对比

#### v20.1

```yaml
# 静态配置
skills:
  - name: "scientific-writing"
    triggers: ["论文", "学术"]
    # 固定不变的配置
```

**限制**：
- ❌ 无法自我学习
- ❌ 无法适应环境
- ❌ 无法协作进化

#### v21.0

```yaml
# 动态进化
evolution_protocol:
  self_learning:
    usage_tracking:
      metrics: ["frequency", "success_rate", "satisfaction"]
    pattern_optimization:
      algorithm: "bandit"
      exploration_rate: 0.1

  environmental_adaptation:
    project_detection:
      indicators: ["project_type", "tech_stack"]
    convention_learning:
      conventions: ["代码风格", "提交格式"]
```

**优势**：
- ✅ 自我学习优化
- ✅ 环境自动适应
- ✅ 协作进化能力

---

### 3. Skills-n8n 协作

#### v20.1

```yaml
# 不明确的协作
mcp:
  servers:
    n8n-mcp:
      status: "enabled"
  # 如何协作？不清楚
```

**问题**：
- ❌ 协作边界模糊
- ❌ 智能路由缺失
- ❌ 模板不明确

#### v21.0

```yaml
# 明确的协作
skills_n8n_collaboration:
  intelligent_router:
    decision_tree:
      - question: "需要持久运行？"
        yes: "n8n"
        no: "继续判断"
      - question: "需要智能决策？"
        yes: "skills"
        no: "hybrid"

  collaboration_templates:
    - name: "plan_and_execute"
      description: "Skills 规划，n8n 执行"
    - name: "trigger_and_process"
      description: "n8n 触发，Skills 处理"
```

**优势**：
- ✅ 明确能力边界
- ✅ 智能路由决策
- ✅ 可复用模板

---

## 迁移步骤

### 步骤 1：备份现有配置

```bash
# 备份当前配置
cd C:/Users/13466/.claude/mindsymphony-v15.6

# 创建备份
cp mindsymphony.config.yml mindsymphony.config.backup.v20.yml
cp -r memory memory.backup.v20
```

### 步骤 2：获取 v21.0 配置

```bash
# 复制新配置文件
cp mindsymphony-v21.0.config.yml mindsymphony.config.yml
```

### 步骤 3：迁移自定义技能

如果你有自定义技能，需要更新触发配置：

**旧格式 (v20.1)**：
```yaml
skills:
  - name: "my-custom-skill"
    triggers: ["关键词1", "关键词2"]
```

**新格式 (v21.0)**：
```yaml
unified_triggers:
  skill_triggers:
    - skill: "my-custom-skill"
      priority: "medium"
      triggers:
        - type: "command"
          patterns: ["/my"]
          weight: 10.0
        - type: "semantic"
          intent: "my_task"
          examples: ["帮我做X", "执行Y任务"]
          weight: 8.0
        - type: "pattern"
          regex: "关键词1|关键词2"
          weight: 5.0
```

### 步骤 4：启用进化功能

```yaml
# 在配置中启用
evolution_protocol:
  enabled: true

  # 创建进化数据库目录
  self_learning:
    usage_tracking:
      path: "C:/Users/13466/.claude/mindsymphony-v21/evolution/learning.db"
```

### 步骤 5：配置 n8n 协作（可选）

如果你使用 n8n，配置 MCP 桥接：

```yaml
skills_n8n_collaboration:
  enabled: true

  mcp_bridge:
    n8n_mcp_server:
      endpoint: "http://localhost:5678/mcp"
```

### 步骤 6：测试迁移

```bash
# 启动 MindSymphony
# 指定新配置
claude-code --config mindsymphony-v21.0.config.yml

# 测试基本功能
# 1. 测试命令触发
用户: /paper

# 2. 测试语义触发
用户: 帮我写一篇研究论文

# 3. 测试模式触发
用户: 分析这个项目的学术价值
```

### 步骤 7：监控进化指标

```bash
# 检查学习数据库
sqlite3 C:/Users/13466/.claude/mindsymphony-v21/evolution/learning.db

# 查询使用统计
SELECT skill_name, invocation_count, success_rate
FROM usage_stats
ORDER BY invocation_count DESC
LIMIT 10;
```

---

## 功能详解

### 1. 统一触发层详解

#### 触发优先级

```
用户输入 → 触发器匹配 → 优先级排序 → 最佳技能

优先级顺序:
1. explicit_command (权重 10.0)  - 最快
2. semantic_match    (权重 8.0)   - 最智能
3. pattern_match     (权重 5.0)   - 最通用
4. context_inference (权重 3.0)   - 最渐进
```

#### 实际示例

```yaml
# 场景：科学写作技能
- skill: "scientific-writing"
  triggers:
    # 级别1：命令（最快响应）
    - type: "command"
      patterns: ["/paper"]
      → 用户输入 "/paper" → 立即触发

    # 级别2：语义（智能理解）
    - type: "semantic"
      intent: "academic_writing"
      examples: ["帮我写论文"]
      → 用户输入 "帮我写一篇关于机器学习的论文"
      → 语义匹配 → 触发

    # 级别3：模式（兜底覆盖）
    - type: "pattern"
      regex: "论文|学术"
      → 用户输入 "这个学术研究..."
      → 关键词匹配 → 触发
```

#### 学习优化

```yaml
learning:
  track_usage: true           # 追踪每次调用
  optimize_patterns: true     # 自动优化模式
  user_adaptation: true       # 适应用户偏好

# 自动学习示例:
# 1. 发现用户常用 "帮我写paper" (中英混合)
# 2. 自动添加到模式库
# 3. 提高该模式权重
```

---

### 2. 进化协议详解

#### 自我学习循环

```
┌─────────────────────────────────────────┐
│          自我学习循环                    │
├─────────────────────────────────────────┤
│                                         │
│  用户请求                                │
│    ↓                                    │
│  技能执行                                │
│    ↓                                    │
│  结果追踪                                │
│    ↓                                    │
│  模式优化 ←──────────────┐              │
│    ↓                     │              │
│  权重调整                │              │
│    ↓                     │              │
│  下次优化 ←──────────────┘              │
│                                         │
└─────────────────────────────────────────┘
```

#### 环境适应示例

```yaml
# 项目检测
project_detection:
  # 检测项目类型
  - 扫描 package.json → 发现 "react"
  → 标记为 "web 项目"
  → 优先推荐前端技能

  # 检测技术栈
  - 扫描 requirements.txt → 发现 "torch"
  → 标记为 "ML 项目"
  → 优先推荐机器学习技能

  # 检测团队规范
  - 分析 git log → 提交消息遵循 Conventional Commits
  → 学习该规范
  → 后续生成符合规范的提交消息
```

#### 协作进化示例

```yaml
# 协同分析
synergy_analysis:
  # 发现协同模式
  - "scientific-writing" 和 "citation-management"
    经常一起调用
    ↓
  创建组合模板
    ↓
  优化调用顺序
    ↓
  提升整体效率
```

---

### 3. Skills-n8n 协作详解

#### 决策树

```python
def route_task(task):
    """智能路由决策"""

    # 问题1：需要持久运行？
    if needs_persistent_execution(task):
        return "n8n"

    # 问题2：需要定时触发？
    if needs_scheduled_trigger(task):
        return "n8n"

    # 问题3：需要复杂外部集成？
    if needs_complex_integration(task):
        return "n8n"

    # 问题4：需要智能决策？
    if needs_cognitive_decision(task):
        return "skills"

    # 默认：混合模式
    return "hybrid"
```

#### 协作模板

**模板1：智能生成 + 自动分发**

```
场景：每周生成研究摘要并发送给团队

[Skills 阶段]
1. scientific-writing: 生成摘要
2. citation-management: 整理引用

[n8n 阶段]
3. schedule: 每周五下午5点
4. email: 发送给团队
5. storage: 备份到云存储
```

**模板2：智能监控 + 告警**

```
场景：监控 API 性能，异常时智能分析

[n8n 阶段]
1. http_request: 每5分钟检查
2. condition: 响应时间 > 1秒？

[Skills 阶段]
3. knowledge-explorer: 分析瓶颈
4. codebase-ecologist: 提供优化建议
```

**模板3：数据处理管道**

```
场景：收集数据并智能分析

[n8n 阶段]
1. webhook: 接收数据
2. queue: 缓冲队列

[Skills 阶段]
3. exploratory-data-analysis: EDA
4. scientific-visualization: 可视化

[n8n 阶段]
5. database: 存储结果
6. notification: 发送通知
```

---

## 最佳实践

### 1. 触发配置最佳实践

#### ✅ 推荐：多维度覆盖

```yaml
- skill: "my-skill"
  triggers:
    - type: "command"      # 快速入口
      patterns: ["/my"]
    - type: "semantic"     # 智能理解
      intent: "my_task"
    - type: "pattern"      # 兜底覆盖
      regex: "关键词"
```

#### ❌ 避免：单一模式

```yaml
- skill: "my-skill"
  triggers:
    - type: "pattern"
      regex: "关键词"
  # 问题：覆盖不全，无法智能匹配
```

### 2. 进化配置最佳实践

#### ✅ 推荐：渐进启用

```yaml
# 第一阶段：只启用追踪
self_learning:
  usage_tracking:
    enabled: true
  pattern_optimization:
    enabled: false  # 先观察

# 第二阶段：收集足够数据后启用
pattern_optimization:
  enabled: true
  min_sample_size: 100
```

#### ❌ 避免：全量启用

```yaml
# 全部启用可能导致不稳定
self_learning:
  usage_tracking:
    enabled: true
  pattern_optimization:
    enabled: true
    exploration_rate: 0.5  # 太高
```

### 3. 协作配置最佳实践

#### ✅ 推荐：明确边界

```yaml
# Skills 处理智能任务
skills_excellence:
  - "内容生成"
  - "决策推理"

# n8n 处理操作任务
n8n_excellence:
  - "定时执行"
  - "外部集成"
```

#### ❌ 避免：职责模糊

```yaml
# 不要让 n8n 做智能决策
# 不要让 Skills 做定时任务
```

---

## 常见问题

### Q1: v21.0 向后兼容吗？

**A**: 部分兼容。

- ✅ 技能定义兼容
- ✅ MCP 集成兼容
- ⚠️ 配置格式需要迁移
- ❌ skill_router 和 commands 被合并

### Q2: 如何平滑迁移？

**A**: 建议分阶段：

```
阶段1: 并行运行
- 保留 v20.1 配置
- 测试 v21.0 配置
- 对比结果

阶段2: 灰度切换
- 部分功能使用 v21.0
- 其他保持 v20.1
- 逐步扩大

阶段3: 完全切换
- 全面使用 v21.0
- 移除 v20.1 配置
```

### Q3: 进化功能会影响性能吗？

**A**: 影响很小。

- 追踪：异步写入，<1ms
- 优化：后台进行，不阻塞
- 学习：增量更新，可控制频率

### Q4: n8n 协作必须配置吗？

**A**: 不是必须的。

- ✅ 不用 n8n：v21.0 依然完全可用
- ✅ 配置 n8n：获得额外自动化能力
- 建议：有自动化需求时再配置

### Q5: 如何禁用某些功能？

**A**: 在配置中设置 `enabled: false`：

```yaml
evolution_protocol:
  enabled: false  # 禁用进化功能

skills_n8n_collaboration:
  enabled: false  # 禁用 n8n 协作
```

### Q6: 如何回滚到 v20.1？

**A**: 恢复备份即可：

```bash
# 恢复配置
cp mindsymphony.config.backup.v20.yml mindsymphony.config.yml

# 恢复记忆（可选）
cp -r memory.backup.v20 memory
```

---

## 迁移检查清单

### 准备阶段

- [ ] 备份现有配置文件
- [ ] 备份记忆数据库
- [ ] 记录当前自定义技能
- [ ] 评估迁移影响范围

### 执行阶段

- [ ] 获取 v21.0 配置文件
- [ ] 迁移自定义技能配置
- [ ] 启用进化功能
- [ ] 配置 n8n 协作（如需要）
- [ ] 测试基本功能
- [ ] 测试触发机制
- [ ] 测试进化学习

### 验证阶段

- [ ] 验证所有技能可正常调用
- [ ] 验证命令触发有效
- [ ] 验证语义匹配准确
- [ ] 验证进化数据正常记录
- [ ] 验证 n8n 协作正常（如配置）

### 优化阶段

- [ ] 监控触发准确率
- [ ] 优化触发模式
- [ ] 调整进化参数
- [ ] 创建协作模板

---

## 下一步

1. **立即开始**：
   ```bash
   # 备份并迁移
   cp mindsymphony.config.yml mindsymphony.config.backup.v20.yml
   cp mindsymphony-v21.0.config.yml mindsymphony.config.yml
   ```

2. **测试体验**：
   - 尝试命令触发：`/paper`
   - 尝试语义触发：`"帮我写论文"`
   - 观察进化数据积累

3. **深入学习**：
   - 阅读 `WORKFLOWS_V21.md`（待创建）
   - 查看 `EVOLUTION_METRICS.md`（待创建）
   - 探索协作模板

---

**祝你升级顺利！🎉**

如有问题，参考：
- 配置文件：`mindsymphony-v21.0.config.yml`
- 架构设计：`MINDSYMPHONY_V21_ARCHITECTURE.md`（待创建）
- 实战案例：`COLLABORATION_PATTERNS.md`（待创建）
